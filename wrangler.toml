name = "a-rehman-sons"
compatibility_date = "2024-09-01"
pages_build_output_dir = "./dist"

# Note: Pages CLI deploy does not support [build] here. Local deploy runs build.js
# via package.json. Set "npm run build" in the Cloudflare dashboard for Git CI.
