{
  "name": "a-rehman-sons-website",
  "version": "1.0.0",
  "description": "A. Rehman & Sons Commercial Chemical Solutions",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && npx wrangler@3 pages deploy dist --project-name=arschemicals --branch=main",
    "deploy:vercel": "npx vercel --prod"
  }
}
