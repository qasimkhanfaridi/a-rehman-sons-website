{
  "name": "a-rehman-sons-website",
  "version": "1.0.0",
  "description": "A. Rehman & Sons Commercial Chemical Solutions",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && npx wrangler@3 pages deploy dist --project-name=a-rehman-sons --branch=main --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "3.114.17"
  }
}
