{
  "name": "a-rehman-sons-website",
  "version": "1.0.0",
  "description": "A. Rehman & Sons Commercial Chemical Solutions",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && npx wrangler pages deploy dist --project-name=a-rehman-sons --branch=main",
    "deploy:vercel": "npx vercel --prod"
  }
}
